/**
 * Daily protein goal (SDD 08). Hardcoded for the MVP; the home screen reads
 * only this constant, so a future settings screen can replace it without
 * touching the UI.
 */
export const DAILY_PROTEIN_GOAL_GRAMS = 150;
